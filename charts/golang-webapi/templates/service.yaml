apiVersion: v1
kind: Service
metadata:
  name: {{ include "golang-webapi.fullname" . }}
  namespace: {{ include "golang-webapi.namespace" . }}
  labels:
    {{- include "golang-webapi.labels" . | nindent 4 }}
  {{- with .Values.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "golang-webapi.selectorLabels" . | nindent 4 }}
