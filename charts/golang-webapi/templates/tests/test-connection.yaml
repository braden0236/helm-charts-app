apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "golang-webapi.fullname" . }}-test-connection"
  labels:
    {{- include "golang-webapi.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "golang-webapi.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
